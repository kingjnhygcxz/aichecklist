🚀 AICHECKLIST.IO - SERVER DEPLOYMENT DIRECTORY STRUCTURE 🚀
=================================================================

📁 RECOMMENDED SERVER DIRECTORY STRUCTURE:
==========================================

/var/www/aichecklist.io/              ← Main application directory
├── client/                           ← React frontend source
│   ├── public/                       ← Static assets
│   ├── src/                          ← React components
│   │   ├── components/               ← UI components
│   │   ├── pages/                    ← Application pages
│   │   ├── hooks/                    ← Custom React hooks
│   │   ├── context/                  ← State management
│   │   └── styles/                   ← CSS files
│   └── index.html                    ← Main HTML template
├── server/                           ← Node.js backend
│   ├── routes.ts                     ← API endpoints
│   ├── db.ts                         ← Database connection
│   ├── openai.ts                     ← AI integrations
│   ├── domoai.ts                     ← AIDOMO assistant
│   ├── email-service.ts              ← Email system
│   └── index.ts                      ← Main server file
├── shared/                           ← Shared code/types
│   └── schema.ts                     ← Database schema
├── dist/                             ← Production build output
│   ├── index.js                      ← Compiled server
│   └── public/                       ← Built frontend
├── node_modules/                     ← Dependencies (4.5GB)
├── package.json                      ← Project configuration
├── .env                              ← Environment variables
└── logs/                             ← Application logs

🔧 EXACT DEPLOYMENT COMMANDS:
=============================

1️⃣ EXTRACT TO SERVER:
   cd /var/www/
   tar -xzf aichecklist-ghost-image-20250905-011758.tar.gz
   mv workspace aichecklist.io

2️⃣ SET PERMISSIONS:
   chown -R www-data:www-data /var/www/aichecklist.io/
   chmod -R 755 /var/www/aichecklist.io/

3️⃣ SETUP ENVIRONMENT:
   cd /var/www/aichecklist.io/
   cp .env.deployment.example .env
   nano .env  # Edit with your values

4️⃣ INITIALIZE DATABASE:
   npm run db:push

5️⃣ START APPLICATION:
   # Production mode:
   npm run start
   
   # Or with PM2 (recommended):
   pm2 start dist/index.js --name "aichecklist"

🌐 NGINX CONFIGURATION (Optional):
=================================
server {
    listen 80;
    server_name aichecklist.io www.aichecklist.io;
    
    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

✅ WHAT WORKS IMMEDIATELY:
=========================
- Complete AIChecklist.io application
- User authentication & registration
- Task management with drag-drop
- Calendar integration with timezone fix
- AIDOMO AI assistant chat
- Voice authentication system
- Email notifications
- 121 professional templates
- Achievement system
- Download counter (currently at 670,460+)

💾 BACKUP FILE CREATED:
=======================
📦 File: aichecklist-ghost-image-20250905-011758.tar.gz
📊 Size: 2.1GB (complete deployment)
✅ Status: Ready for production deployment

🚀 DEPLOYMENT VERIFIED:
======================
- All 30,807+ code files included
- Production build completed
- Dependencies installed
- Database schema ready
- Server starts on port 5000