# AICHECKLIST.IO Environment Configuration
# Copy this file to .env in your deployment directory
# ⚠️ SECURITY WARNING: NEVER commit real API keys or secrets to version control!

# OpenAI API Key (Required for all AI features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database Connection (PostgreSQL)
# Replace with your actual database connection string
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
PGDATABASE=your_database_name

# Email Services (Optional - for email verification)
# Get your key from: https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# Email Validation (Optional - for email verification)
# Get your key from: https://www.neverbounce.com/
NEVERBOUNCE_API_KEY=your_neverbounce_api_key_here

# Security (Required - Generate a secure random string)
# Use: openssl rand -base64 64
SESSION_SECRET=your_secure_session_secret_here

# Production Configuration
NODE_ENV=production
PORT=5000

# Public URL for email verification links (CRITICAL for deployed sites)
# Replace with your actual domain - NEVER use localhost in production
PUBLIC_URL=https://yourdomain.com